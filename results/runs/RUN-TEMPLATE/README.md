# RUN-TEMPLATE

This folder is a non-evidence template. Copy its structure when creating a real run folder.

Use a real run name such as:

```text
RUN-YYYYMMDD-001
```

A real run is not evidence until the folder contains completed logs, raw data, hashes, an evidence manifest, and reviewer signoff.
